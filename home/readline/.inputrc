## Configuration for readline (used by bash and other shells)

# Include system default
$include /etc/inputrc


## Tab completion

# Make Tab cycle through completions instead of listing
"\t": menu-complete
# Make Shift-Tab cycle backwards through completions
"\e[Z": menu-complete-backward

# Show completion list immediately
set show-all-if-ambiguous on
# Show prefix before cycling
set menu-complete-display-prefix on

# Ignore case when completing
set completion-ignore-case on
# Better behavior with case-insensitive completion
set completion-map-case on

# Append a slash to completed directory names
set mark-directories on
set mark-symlinked-directories on


## History search

# Make up/down search through history based on current input
"\e[A": history-search-backward
"\e[B": history-search-forward
# Prevent cursor from moving when navigating history
set history-preserve-point on


## Other

# Bracketed paste mode (reduces pasted text being misinterpreted)
set enable-bracketed-paste on
