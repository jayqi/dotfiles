# Added by dbt Fusion extension (ensure dbt binary dir on PATH)
if [[ ":$PATH:" != *":/Users/jay/.local/bin:"* ]]; then
  export PATH=/Users/jay/.local/bin:"$PATH"
fi
# Added by dbt Fusion extension
alias dbtf=/Users/jay/.local/bin/dbt

# pnpm
export PNPM_HOME="/Users/jay/.local/share/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end
